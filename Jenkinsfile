pipeline {
    agent any
    environment {
        name = 'Naani'
        course = 'k8s'
    }
    stages {
        stage ('Build') {
            steps {
                echo "Welcome ${name}"
                echo "you enrolled to ${course} course"
                //echo "you are certified in gcp"
            }
        }
    }
}
